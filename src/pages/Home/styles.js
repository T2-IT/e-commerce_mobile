import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  text: {
    color: '#000',
    fontSize: 25,
  },
  container: {
    flex: 1,
    backgroundColor: '#FFF',
    padding: 20,
  },
});
export default styles;
